#include "config.h"

DEFINE_uint64(DS_MB, 100, "Datasize set");
DEFINE_uint64(HEAP_SIZE, 0, "must be based by script");
DEFINE_bool(SEQ, true, "order sequential or random");
DEFINE_uint64(SECONDS, 10, "runnging for seconds");
